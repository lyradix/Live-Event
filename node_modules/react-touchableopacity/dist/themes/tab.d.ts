/** @jsxImportSource @emotion/react */
import { Interpolation, Theme } from "@emotion/react";
import { TabType } from "../types/tab";
export declare const TabTheme: ({ direction, reverse, gap, crossGap, border, borderRadius, axis, ...props }: TabType) => Interpolation<Theme>;
